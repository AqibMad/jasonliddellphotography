{#
/**
 * @file
 * Testimonials page template.
 *
 * IMPORTANT:
 * - Keep this template "markup-only" (no inline styles / inline JS).
 * - Slider behavior and layout are handled by theme CSS + JS libraries.
 */
#}

<div class="mobile-overlay" id="mobileOverlay"></div>

<div class="container page--testimonials">
  {{ page.sidebar_first }}

  <main class="content-panel content-panel--testimonials" role="main">
    <div class="content-panel__inner">
      {{ page.content }}
    </div>
  </main>
</div>
