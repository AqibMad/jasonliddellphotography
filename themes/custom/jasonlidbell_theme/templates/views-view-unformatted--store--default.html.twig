{#
/**
 * @file
 * Views unformatted template for store products grid
 * Each row is already rendered as a product card by the teaser template
 */
#}

{% for row in rows %}
  {{ row.content }}
{% endfor %}
